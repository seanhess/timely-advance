{
  "name": "web",
  "version": "1.0.0",
  "description": "web",
  "main": "build/elm.js",
  "scripts": {
    "test": "echo \"test\"",
    "start": "node_modules/.bin/elm-live src/Main.elm -d build --proxyHost http://localhost:3001 --proxyPrefix /v1 --pushstate --open -- --debug --output=build/elm.js",
    "build": "node_modules/.bin/elm make src/Main.elm --output=build/elm.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "elm": "^0.19.0-bugfix6",
    "elm-live": "^3.4.0"
  }
}
